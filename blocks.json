{
  "catalog_version": "1.0",
  "generated_by": "hand-written; the same format `signalcraft catalog` writes",
  "note": "The C++ blocks this library ships. C# blocks need no entry here - the studio compiles their source and reads what the class declares. Nothing can ask a compiled C++ binary what it contains, so that half is written down. The `cpp` object is what `signalcraft build --emit cpp` needs to construct the block: the class to make and the header to include.",

  "categories": [
    "filters"
  ],

  "blocks": [
    {
      "type": "CommunityNotch",
      "origin": "native",
      "language": "cpp",
      "category": "filters",
      "label": "Notch",
      "doc": "Removes one frequency and leaves the rest - mains hum, a leaking pilot, a birdie from a mixer.",
      "runtime_name": "Notch",
      "constructibility": "default",
      "needs_hardware": false,
      "native_emittable": true,

      "cpp": {
        "class": "community::NotchBlock",
        "header": "community/notch.hpp",
        "ctor_style": 0
      },

      "ctor_fields": [],

      "ports": {
        "shape": "fixed",
        "inputs": [
          { "id": "in", "label": "In", "type": "Float32", "index": 0 }
        ],
        "outputs": [
          { "id": "out", "label": "Out", "type": "Float32", "index": 0 }
        ]
      },

      "params": [
        {
          "id": "sample_rate",
          "label": "Sample rate",
          "type": "double",
          "access": "readwrite",
          "min": 1.0,
          "max": 100000000.0
        },
        {
          "id": "frequency",
          "label": "Frequency",
          "type": "double",
          "access": "readwrite",
          "min": 1.0,
          "max": 1000000.0
        },
        {
          "id": "width",
          "label": "Width",
          "type": "double",
          "access": "readwrite",
          "min": 0.1,
          "max": 10000.0
        }
      ]
    }
  ]
}
